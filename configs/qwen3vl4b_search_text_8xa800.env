export EXPERIMENT_VARIANT="${EXPERIMENT_VARIANT:-search_text}"
export SEED="${SEED:-0}"

export MODEL_PATH="${MODEL_PATH:-/hpc2hdd/home/rtang906/hf_models/Qwen3-VL-4B-Instruct}"
export TRAIN_FILE="${TRAIN_FILE:-$HOME/data/searchR1_processed_direct/train.parquet}"
export VAL_FILE="${VAL_FILE:-$HOME/data/searchR1_processed_direct/test.parquet}"
export TRAINER_PROJECT_NAME="${TRAINER_PROJECT_NAME:-AgentOCR_search}"
export TRAINER_EXPERIMENT_NAME="${TRAINER_EXPERIMENT_NAME:-search_text_qwen3vl4b_7train1retr_8xa800_s${SEED}}"

export PYTHON_BIN="${PYTHON_BIN:-/hpc2hdd/home/rtang906/AgentOCR/tools/python_glibc234}"
export VERL_HF_ATTN_IMPLEMENTATION="${VERL_HF_ATTN_IMPLEMENTATION:-sdpa}"
export PYTHONNOUSERSITE="${PYTHONNOUSERSITE:-1}"

export TRAIN_DATA_SIZE="${TRAIN_DATA_SIZE:-14}"
export VAL_DATA_SIZE="${VAL_DATA_SIZE:-128}"
export GROUP_SIZE="${GROUP_SIZE:-8}"
export RAY_INIT_NUM_CPUS="${RAY_INIT_NUM_CPUS:-56}"

export DATA_MAX_PROMPT_LENGTH="${DATA_MAX_PROMPT_LENGTH:-14000}"
export DATA_MAX_RESPONSE_LENGTH="${DATA_MAX_RESPONSE_LENGTH:-512}"
export ENV_MAX_STEPS="${ENV_MAX_STEPS:-4}"
export ENV_HISTORY_LENGTH="${ENV_HISTORY_LENGTH:-4}"

export ACTOR_PPO_MINI_BATCH_SIZE="${ACTOR_PPO_MINI_BATCH_SIZE:-112}"
export ACTOR_PPO_MICRO_BATCH_SIZE_PER_GPU="${ACTOR_PPO_MICRO_BATCH_SIZE_PER_GPU:-2}"
export ACTOR_LR="${ACTOR_LR:-1e-6}"
export ROLLOUT_LOG_PROB_MICRO_BATCH_SIZE_PER_GPU="${ROLLOUT_LOG_PROB_MICRO_BATCH_SIZE_PER_GPU:-4}"
export ROLLOUT_GPU_MEMORY_UTILIZATION="${ROLLOUT_GPU_MEMORY_UTILIZATION:-0.35}"
export ROLLOUT_ENFORCE_EAGER="${ROLLOUT_ENFORCE_EAGER:-False}"
export ROLLOUT_FREE_CACHE_ENGINE="${ROLLOUT_FREE_CACHE_ENGINE:-False}"
export VLLM_SKIP_MM_PROFILING="${VLLM_SKIP_MM_PROFILING:-True}"

export ACTOR_STRATEGY="${ACTOR_STRATEGY:-fsdp2}"
export ACTOR_USE_TORCH_COMPILE="${ACTOR_USE_TORCH_COMPILE:-False}"
export ACTOR_ENABLE_ACTIVATION_OFFLOAD="${ACTOR_ENABLE_ACTIVATION_OFFLOAD:-False}"
export ACTOR_FSDP_PARAM_OFFLOAD="${ACTOR_FSDP_PARAM_OFFLOAD:-False}"
export ACTOR_FSDP_OPTIMIZER_OFFLOAD="${ACTOR_FSDP_OPTIMIZER_OFFLOAD:-False}"
export ACTOR_FSDP_OFFLOAD_POLICY="${ACTOR_FSDP_OFFLOAD_POLICY:-False}"

export SEARCH_MAX_CONCURRENT_REQUESTS="${SEARCH_MAX_CONCURRENT_REQUESTS:-4}"
export SEARCH_ENV_MAX_WORKERS="${SEARCH_ENV_MAX_WORKERS:-4}"
export SEARCH_BATCH_REQUEST_SIZE="${SEARCH_BATCH_REQUEST_SIZE:-16}"
export SEARCH_SERVER_CUDA_VISIBLE_DEVICES="${SEARCH_SERVER_CUDA_VISIBLE_DEVICES:-7}"

export TRAINER_N_GPUS_PER_NODE="${TRAINER_N_GPUS_PER_NODE:-7}"
export TRAINER_NNODES="${TRAINER_NNODES:-1}"
export TRAINER_SAVE_FREQ="${TRAINER_SAVE_FREQ:-10}"
export TRAINER_TEST_FREQ="${TRAINER_TEST_FREQ:-150}"
export TRAINER_TOTAL_TRAINING_STEPS="${TRAINER_TOTAL_TRAINING_STEPS:-150}"
export TRAINER_VAL_BEFORE_TRAIN="${TRAINER_VAL_BEFORE_TRAIN:-False}"
